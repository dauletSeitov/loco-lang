import std.sortString
import std.maxElement
import std.sort
import std.stringToList
import std.listToString
import std.upperCase
import std.lowerCase
import std.trim
import std.reverseList
import std.uniqueList
import std.contains
import std.indexOf
import std.isEven
import std.map
import std.filter as ok
import custom.fibonacci as customfibonacci

fun main() {


    var user = <name: "bagdaulet", age: 45, contacts: ["phone", "phone 2"]>
    
    println("user name: " + user.name)
    println("user age: " + user["age"])
    println("contacts: " + user.contacts)


}
